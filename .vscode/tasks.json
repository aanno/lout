{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "go test (debug)",
            "type": "shell",
            "command": "go",
            "args": [
                "test",
                "-c",
                "-o",
                "${workspaceFolder}/__debug_bin"
            ],
            "options": {
                "cwd": "${workspaceFolder}",
            }
        }
    ]
}
